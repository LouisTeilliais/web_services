name: Deploy NestJS to Azure App Service

on:
  push:
    branches:
      - deployement  # Déclenche le workflow sur push dans la branche main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 📦 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"

      - name: 📌 Install dependencies
        run: npm install

      - name: 🔬 Run tests
        run: npm test  # Assurez-vous que la commande de test est correcte pour votre projet

  build-and-deploy:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 📦 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"

      - name: 📌 Install dependencies
        run: npm install

      - name: 🔨 Build the project
        run: npm run build  # S'assure que l'application est compilée

      - name: 🔑 Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}


      - name: 🚀 Deploy to Azure App Service.
        uses: azure/webapps-deploy@v2
        with:
          app-name: "Webservices"  # Remplace avec le nom exact de ton App Service Azure
          package: "."

      - name: ✅ Post-deployment check
        run: echo "✅ Déploiement terminé avec succès !"
